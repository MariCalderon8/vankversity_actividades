/**
 * Cree una función que reciba un arreglo de n elementos y retorne el
 * número de elementos del arreglo.
 */

const countArrayElements = (array) => array.length;

let array = [1, "h", 7, false, 5, 9];
console.log(countArrayElements(array));
